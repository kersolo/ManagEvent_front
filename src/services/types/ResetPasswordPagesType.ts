export type Token = {
  token: string;
};

export type UserId = {
  id: string;
};

export type Inputs = {
  password: string;
  confirmPassword: string;
};

export type Input = {
  email: string;
};
